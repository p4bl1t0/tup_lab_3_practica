import React from "react";

const Title = ({ pageTitle }) => {
  const Title = ({ title }) => {
    return (
      <div>
        <h1>{pageTitle}</h1>
        <h1>{title}</h1>
      </div>
    );
  };
};

export default Title;
