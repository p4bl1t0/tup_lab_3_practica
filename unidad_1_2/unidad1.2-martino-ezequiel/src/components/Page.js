import React from "react";
import Header from "./Header.js";
import Main from "./Main";

const Page = ({ children }) => {
  return (
    <div>
      {children}
    </div>
  );
};

export default Page;
